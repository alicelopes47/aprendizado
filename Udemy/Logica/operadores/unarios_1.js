//! é um operador unário
let a = 1;

a = a + 1; //operador binário (a e 1, operador no meio dos operandos, nome da estratégia: infix)
a += 1; 
a++; //adicionar mais um (postfix: depois do operando)
++a;// operador prefix (antes do operando)
a--;
--a;
console.log(a)