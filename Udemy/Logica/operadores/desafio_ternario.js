const a = 8
const b = 4
const operacao = '*'; 

const resultado = 
    operacao == '+' ? (a + b) :
        operacao == '-' ? (a - b) :
            operacao == '*' ? (a * b) : (a / b)

console.log(resultado)

//foi sofrido, mas consegui!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
//ficar atenta em qual simbolo usar, as vezes so ele pode ser simples.
